# @package _global_
# DPF: 1D Linear-Gaussian model with OT resampling (epsilon=0.5) + gradient through resampling
#
# Simplest possible model — timing baseline.
# Infers obs_noise_std (true: 1.0).
# Gradient flows through Sinkhorn transport matrix.

model:
  _target_: src.models.linear_gaussian.LinearGaussianModel
  F: [[0.9]]
  B: [[1.0]]
  H: [[1.0]]
  D: [[1.0]]            # direction matrix; actual R = obs_noise_std^2 * D@D^T
  obs_noise_std: 2.0    # Initial guess (true: 1.0)

filter:
  _target_: src.filters.particle.ledh_invertible_hmc.LEDHParticleFlowFilterHMC
  n_particles: 500
  n_lambda_steps: 15
  resampling_method: ot_entropy
  resampling_config:
    epsilon: 0.5
    extrapolation_damping: 0.5
  weight_clip_range: 50.0
  stop_gradient_resampling: false
  hmc_resampling_method: ot_entropy
  hmc_resampling_config:
    epsilon: 0.5
    extrapolation_damping: 0.5
  eager_mode: false

dpf:
  sampler: hmc

  trainable_params:
    obs_noise_std:
      constraint: positive
      prior:
        _target_: tensorflow_probability.distributions.LogNormal
        loc: 0.0          # log(1.0) = 0
        scale: 1.0

  hmc:
    num_samples: 200
    num_burnin: 100
    step_size: 0.001
    num_leapfrog_steps: 5
    max_tree_depth: 10
    adaptation_rate: 0.8
    target_accept_prob: 0.65
    seed: 42

data:
  T: 50
  seed: 42
  true_params:
    obs_noise_std: 1.0
