# @package _global_

# Experiment: EDH Invertible Flow on Acoustic Tracking Model

defaults:
  - override /model: acoustic_tracking_full
  - override /filter: edh_invertible

# Override settings
T: 100
seed: 42
plot: true
output_dir: outputs/acoustic_tracking/acoustic_tracking_edh_invertible
