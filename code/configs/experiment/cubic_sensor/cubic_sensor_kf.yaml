# @package _global_

# Experiment: Kalman Filter (linearized at x=0) on Cubic Sensor Model

defaults:
  - override /model: cubic_sensor
  - override /filter: kalman_cubic_sensor

T: 200
seed: 42
plot: true
output_dir: outputs/cubic_sensor/cubic_sensor_kf
