# @package _global_

# Experiment: Bootstrap Particle Filter (OT entropy resampling) on Cubic Sensor Model

defaults:
  - override /model: cubic_sensor
  - override /filter: particle_tf_ot

T: 200
seed: 42
plot: true
output_dir: outputs/cubic_sensor/cubic_sensor_pf_ot
