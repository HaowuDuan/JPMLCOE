# @package _global_

# Debug Test: EDH Invertible Filter

defaults:
  - override /model: acoustic_tracking_debug
  - override /filter: edh_invertible

# Override settings
T: 300
seed: 42
plot: true
output_dir: outputs/debug_test/edh_invertible
