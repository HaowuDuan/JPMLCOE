# @package _global_

# Experiment: EDH Invertible Flow on Range-Bearing Model
# Tests invertible particle flow with resampling on range-bearing tracking

defaults:
  - override /model: range_bearing
  - override /filter: edh_invertible

# Override settings
T: 50
seed: 42
plot: true
output_dir: outputs/range_bearing/range_bearing_edh_invertible









