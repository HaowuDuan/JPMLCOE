# @package _global_

# Experiment: LEDH Invertible Flow on Range-Bearing Model
# Tests local invertible particle flow with resampling on range-bearing tracking

defaults:
  - override /model: range_bearing
  - override /filter: ledh_invertible

# Override settings
T: 100
seed: 42
plot: true
output_dir: outputs/range_bearing/range_bearing_ledh_invertible









