# @package _global_

# Experiment: SDE Local Correction on Range-Bearing Model
# Global linearization with e₀ = h(η̄_0) - H·η̄_0 correction

defaults:
  - override /model: range_bearing
  - override /filter: sde_local_correction

# Override settings
T: 100
seed: 42
plot: true
diffusion_scale: 0.00
output_dir: outputs/range_bearing/range_bearing_sde_local_correction
