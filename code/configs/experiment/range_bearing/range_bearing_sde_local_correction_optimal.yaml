# @package _global_

# Experiment: SDE Local Correction on Range-Bearing Model (optimal schedule)
# Global linearization with e₀ correction, optimal β(λ) schedule

defaults:
  - override /model: range_bearing
  - override /filter: sde_local_correction

# Override settings
T: 100
seed: 42
plot: true
output_dir: outputs/range_bearing/range_bearing_sde_local_correction_optimal

# Override filter parameters
filter:
  schedule_mu: 1.0
