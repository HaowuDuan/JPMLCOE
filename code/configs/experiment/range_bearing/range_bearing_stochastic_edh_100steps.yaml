# @package _global_

# Experiment: Stochastic EDH on Range-Bearing Model (100 lambda steps)
# Tests stochastic EDH with more steps to reduce overshooting

defaults:
  - override /model: range_bearing
  - override /filter: stochastic_edh

# Override settings
T: 100
seed: 42
plot: true
diffusion_scale: 0.00
n_lambda_steps: 100
output_dir: outputs/range_bearing/range_bearing_stochastic_edh_100steps
