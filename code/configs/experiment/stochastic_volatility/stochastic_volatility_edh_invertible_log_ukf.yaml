# @package _global_

# Experiment: EDH Invertible on SV (log-space) with UKF global filter

defaults:
  - override /model: stochastic_volatility_log
  - override /filter: edh_invertible

T: 100
seed: 42
plot: true
output_dir: outputs/stochastic_volatility/stochastic_volatility_edh_invertible_log_ukf

filter:
  filter_type: ukf
