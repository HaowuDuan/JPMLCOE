# @package _global_

# Experiment: Kernel Flow Filter (Scalar) on Stochastic Volatility Model
# Tests scalar kernel particle flow on nonlinear observation model

defaults:
  - override /model: stochastic_volatility
  - override /filter: kernel_flow_scalar

# Override settings
T: 100
seed: 42
plot: true
output_dir: outputs/stochastic_volatility/stochastic_volatility_kernel_scalar

# Override filter parameters for stochastic volatility
filter:
  max_iter: 2000

