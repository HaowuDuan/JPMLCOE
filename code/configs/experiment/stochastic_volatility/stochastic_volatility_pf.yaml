# @package _global_

# Experiment: Bootstrap Particle Filter on Stochastic Volatility Model
# Tests particle filter performance on nonlinear observation model with multiplicative noise

defaults:
  - override /model: stochastic_volatility
  - override /filter: particle

# Override settings
T: 500
seed: 42
plot: true
output_dir: outputs/stochastic_volatility/stochastic_volatility_pf
