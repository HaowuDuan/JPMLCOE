# @package _global_

# Experiment: EDH Flow on Two-Sensor Bearing-Only Model
# Tests flow filter on stiff problem with anisotropic prior

defaults:
  - override /model: two_sensor_bearing
  - override /filter: edh_flow

# Override settings
T: 1
seed: 42
plot: true
output_dir: outputs/two_sensor_bearing/two_sensor_bearing_edh_flow

