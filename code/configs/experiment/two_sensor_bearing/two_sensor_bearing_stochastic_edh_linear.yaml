# @package _global_

# Experiment: Stochastic EDH with Linear Schedule on Two-Sensor Bearing-Only Model
# Uses β(λ) = λ, β̇ = 1 (straightline schedule)

defaults:
  - override /model: two_sensor_bearing
  - override /filter: stochastic_edh

# Override settings
T: 1
seed: 42
plot: true
output_dir: outputs/two_sensor_bearing/two_sensor_bearing_stochastic_edh_linear

