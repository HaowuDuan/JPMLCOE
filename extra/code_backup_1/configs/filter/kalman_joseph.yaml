# Kalman Filter with Joseph Form Covariance Update
# More numerically stable for ill-conditioned problems

_target_: src.filters.kalman.kalman.KalmanFilter

# Matrices extracted from model
F: ${model.F}
B: ${model.B}
H: ${model.H}
D: ${model.D}
mean_0: ${model.mu_0}
Sigma_0: ${model.Sigma_0}

use_joseph_form: true
