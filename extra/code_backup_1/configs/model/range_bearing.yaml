# Range-Bearing Model Configuration

_target_: src.models.range_bearing.RangeBearingModel

# Sensor position
# sensor_pos: [0.0, 0.0]

# Noise parameters
sigma_range: 0.1      # Range measurement noise std
sigma_bearing: 0.01   # Bearing measurement noise std (radians)

# Process and observation noise will use defaults in model
