# @package _global_

# Experiment: Kernel Flow Filter (Matrix-Valued) on Stochastic Volatility Model
# Tests matrix-valued kernel particle flow on nonlinear observation model

defaults:
  - override /model: stochastic_volatility
  - override /filter: kernel_flow_matrix

# Override settings
T: 500
seed: 42
plot: true
output_dir: outputs/stochastic_volatility/stochastic_volatility_kernel_matrix

# Override filter parameters for stochastic volatility
filter:
  max_iter: 2000

