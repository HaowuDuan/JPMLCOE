# @package _global_

# Experiment: LEDH Flow on Two-Sensor Bearing-Only Model
# Tests local flow filter on stiff problem with anisotropic prior

defaults:
  - override /model: two_sensor_bearing
  - override /filter: ledh_flow

# Override settings
T: 1
seed: 42
plot: true
output_dir: outputs/two_sensor_bearing/two_sensor_bearing_ledh_flow

