_target_: src.filters.particle.stochastic_edh.StochasticEDHFlow

n_particles: 500
n_lambda_steps: 100
diffusion_scale: 0.001  # Scalar q for Q = q*I. 0.0 = deterministic flow
schedule_func: null  # null = linear schedule (β=λ, β̇=1)
n_threads: null  # null = use CPU count






